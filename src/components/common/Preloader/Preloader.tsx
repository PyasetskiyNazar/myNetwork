import preloader from '../../../images/Bee-Hollow-Farm.gif';

const Preloader: React.FC = () => {
    return(
        <img alt="preloader" src={preloader} style={{ width: '70px' }} />
    )
}

export default Preloader;